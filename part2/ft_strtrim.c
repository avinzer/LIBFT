#include "libft.h"

char *ft_strtrim(char const *s1, char const *set);
{
    const char *str;
    int start = 0;
    int end = 0;

    if (!s1)
    {
        return NULL;
    }
    if (!set)
    {
        return s1;
    }

    while (s1&& [start] )
    {
        i++;
    }

    str = malloc(sizeof(char *) * (i + 1));
    if (!str)
    {
        return NULL;
    }
    
    while (s1[j])
    {

    }
}